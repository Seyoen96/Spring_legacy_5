<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.sy.s5.member.MemberDAO">
	  <select id="memberLogin" parameterType="com.sy.s5.member.MemberDTO" resultType="com.sy.s5.member.MemberDTO">
	  	select * from member where id=#{id} and pwd=#{pwd}
	  </select>
	  
	  <insert id="memberJoin" parameterType="com.sy.s5.member.MemberDTO">
	  	insert into member values(#{id}, #{pwd}, #{name}, #{email}, #{phone}, #{age})
	  </insert>
  	
	  <update id="memberUpdate" parameterType="com.sy.s5.member.MemberDTO">
	  	 update member set name=#{name}, email=#{email}, phone=#{phone}, age=#{age} where id=#{id}
	  </update>
	  
	  <delete id="memberDelete" parameterType="java.lang.String">
	  	delete member where id=#{id}
	  </delete>
	  
  </mapper>